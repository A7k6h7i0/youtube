NODE_ENV=development
PORT=3000
FRONTEND_ORIGINS=http://localhost:5173

# Optional: protect the seed endpoint (recommended for production)
# If set in production, you must send `x-seed-secret: <value>` header.
SEED_SECRET=

# Option A (recommended): full connection string
MONGODB_URI=

# Option B: pieces (used if MONGODB_URI is empty)
DB_host=cluster0.lmednm5.mongodb.net
DB_user=
DB_password=
DB_name=youtubeClone

SECRET_KEY=akhila_secret_key_123
EMAIL=myemail@gmail.com
PASSWORD=my_password

# Used to generate reset password links
BACKEND_URL=http://localhost:3000
